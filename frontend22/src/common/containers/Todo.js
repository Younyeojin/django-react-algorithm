import { TodoList, TodoInput } from "common/index"
import React from "react"

export default function Todo(){
    return(
        <div>
            <TodoInput/>
            <TodoList/>
        </div>
    )
}

